/**
 * @format
 * @flow
 */

import React, {useCallback, useMemo, useState} from 'react';
import {Text} from 'react-native';


const Episodes = ({locationUrl}) => {
    return <Text>Hello from Episodes</Text>
};

export default Episodes
